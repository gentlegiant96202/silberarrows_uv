{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "iopaint start --model=lama --device=cpu --port=${PORT:-8080} --host=0.0.0.0"
  }
}

